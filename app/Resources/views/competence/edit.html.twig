{% extends 'baseBackend.html.twig' %}

{% block body %}
    <h1>Competence edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('mes_competences') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
